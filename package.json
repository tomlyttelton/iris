{
  "name": "iris-research",
  "version": "0.1.0",
  "main": "src/index.ts",
  "scripts": {
    "start": "ts-node src/index.ts",
    "build": "tsc",
    "deploy": "npm run build && gcloud functions deploy irisApi --gen2 --runtime=nodejs20 --region=us-central1 --entry-point=irisApi --source=. --trigger-http --allow-unauthenticated"
  },
  "dependencies": {
    "apify": "^3.4.0",
    "axios": "^1.6.0",
    "express": "^4.18.2",
    "openai": "^4.97.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
